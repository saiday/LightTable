name: Build DMG

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build app
        run: |
          xcodebuild build \
            -scheme LightTable \
            -configuration Release \
            -derivedDataPath "$RUNNER_TEMP/DerivedData" \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_ALLOWED=NO

      - name: Prepare app for DMG
        run: |
          mkdir -p "$RUNNER_TEMP/app"
          cp -R "$RUNNER_TEMP/DerivedData/Build/Products/Release/Light Table.app" "$RUNNER_TEMP/app/"

      - name: Create DMG
        run: |
          hdiutil create -volname "Light Table" \
            -srcfolder "$RUNNER_TEMP/app" \
            -ov -format UDZO \
            "$RUNNER_TEMP/LightTable.dmg"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ runner.temp }}/LightTable.dmg
          generate_release_notes: true
